package ru.romanow

/**
 * Дано две строки `text1` и `text2`, вернуть длину самой длинной общей подпоследовательности символов. Если общей
 * подпоследовательности нет, то вернуть 0. Подпоследовательность строки — это новая строка, созданная из исходной
 * строки с удалением некоторых символов (может быть и ни одного) без изменения относительного порядка оставшихся
 * символов.
 *
 * Общая подпоследовательность двух строк — это подпоследовательность, общая для обеих строк.
 *
 * [https://leetcode.com/problems/longest-common-subsequence/](https://leetcode.com/problems/longest-common-subsequence/)
 */
class LongestCommonSubsequence {
    fun longestCommonSubsequence(text1: String, text2: String): Int {
        return 0
    }
}
